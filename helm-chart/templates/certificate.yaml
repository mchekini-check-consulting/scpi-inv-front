apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: scpi-inv-front-tls
spec:
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: lets-encrypt
  secretName: scpi-inv-front-tls
  dnsNames:
    - {{ if .Values.ingressPrefix }}{{ .Values.ingressPrefix }}.{{ end }}scpi-inv.check-consulting.net
  usages:
    - digital signature
    - key encipherment